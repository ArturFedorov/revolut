import {IConfiguration} from '../shared/interfaces/IConfiguration';

export const config: IConfiguration = {
  api: '',
  currencyList: {
    bp: 'British Pound',
    eur: 'Euro',
    usd: 'US Dollar'
  }
}
